# Render sets NODE_ENV=production, which makes plain `npm install` skip
# devDependencies (breaking the TypeScript build). Force dev tooling to
# always install: build inputs (typescript, @types/*) also live in
# "dependencies" as a second layer of defense.
include=dev
